@model List<ClinicCare.Models.ExpenseCategory>

@{
    Layout = "_DashboardLayout";
}

@if (TempData["Success"] != null)
{
    <div class="alert alert-success auto-alert">
        @TempData["Success"]
    </div>
}

@if (TempData["Error"] != null)
{
    <div class="alert alert-danger auto-alert">
        @TempData["Error"]
    </div>
}

<div class="d-flex justify-content-between align-items-center mb-4">

    <div>

        <h2 class="mb-0">
            <i class="bi bi-receipt-cutoff me-2"></i>
            Expense Categories
        </h2>

        <small class="text-muted">
            Manage expense categories used for clinic expenses
        </small>

    </div>

    <a asp-action="Create"
       class="btn btn-success">

        Add Category

    </a>

</div>



        <div class="table-responsive">

            <table class="table table-hover align-middle">

                <thead class="table-light">

                    <tr>

                        <th width="100">
                            ID
                        </th>

                        <th>
                            Expense Name
                        </th>

                        <th width="180">
                            Actions
                        </th>

                    </tr>

                </thead>

                <tbody>

                    @if (Model.Any())
                    {
                        @foreach (var item in Model)
                        {
                            <tr>

                                <td>

                                    <span class="badge bg-primary">

                                        @item.Id

                                    </span>

                                </td>

                                <td>

                                    @item.Name

                                </td>

                                <td>

                                    <a asp-action="Edit"
                                       asp-route-id="@item.Id"
                                       class="btn btn-warning btn-sm">

                                       Edit

                                    </a>

                                    <form asp-action="Delete"
                                          method="post"
                                          style="display:inline;">

                                        <input type="hidden"
                                               name="id"
                                               value="@item.Id" />

                                        <button type="submit"
                                                class="btn btn-danger btn-sm"
                                                onclick="return confirm('Delete this expense category?')">

                                           Delete

                                        </button>

                                    </form>

                                </td>

                            </tr>
                        }
                    }
                    else
                    {
                        <tr>

                            <td colspan="3"
                                class="text-center text-muted py-4">

                                <i class="bi bi-inbox fs-4 d-block mb-2"></i>

                                No expense categories found.

                            </td>

                        </tr>
                    }

                </tbody>

            </table>

        </div>

  

@section Scripts
{
    <script>
        setTimeout(function () {
            $(".auto-alert").fadeOut("slow");
        }, 3000);
    </script>
}